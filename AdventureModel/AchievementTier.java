package AdventureModel;

/**
 * An enum representing the tier of an achievement.
 * Used to recognize differences in the difficulty of achievements.
 * Gold is the hardest, followed by Silver, then Bronze.
 */
public enum AchievementTier {
    Gold, Silver, Bronze
}
